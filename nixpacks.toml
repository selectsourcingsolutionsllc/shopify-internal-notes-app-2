[variables]
NIXPACKS_NO_CACHE = "1"

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci --include=dev --cache=/tmp/.npm"]

[phases.build]
cmds = ["rm -rf build", "npx prisma generate", "npm run build"]

[start]
cmd = "npx prisma migrate deploy && npm run start"