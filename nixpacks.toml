[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --include=dev"]

[phases.build]
cmds = ["npx prisma generate", "npx remix build"]
dependsOn = ["install"]
cacheDirectories = ["/root/.npm"]

providers = []

[start]
cmd = "npm run start"