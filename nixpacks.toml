[variables]
NIXPACKS_NO_CACHE = "1"

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci --include=dev --cache=/tmp/.npm"]

[phases.build]
cmds = ["rm -rf build", "npx prisma generate", "npx prisma migrate deploy", "npx remix build"]

[start]
cmd = "npm run start"