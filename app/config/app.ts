// Centralized app configuration constants
// Single source of truth for the app handle and managed pricing URL

export const APP_HANDLE = "product-notes-for-staff";
export const MANAGED_PRICING_URL = `shopify:admin/charges/${APP_HANDLE}/pricing_plans`;
